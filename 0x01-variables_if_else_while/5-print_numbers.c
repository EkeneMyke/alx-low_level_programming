#include <stdio.h>
/**
 *main - Entry point
 *Description: Print numbers in Base 10
 *Return: Always 0
 */
int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		printf("%d\n", n);
		n++;
	}
	return (0);
}
